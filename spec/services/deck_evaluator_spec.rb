# frozen_string_literal: true

RSpec.describe DeckEvaluator do
  describe '.call' do
    context 'when deck is valid' do
      it 'iterates through known hands'
    end

    context 'when one or more cards is invalid' do
      it 'returns message of invalid deck'
    end
  end
end
